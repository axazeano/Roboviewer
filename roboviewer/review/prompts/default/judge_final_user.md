{context}
# Findings that survived verification ({count})

Each was checked on its own; what that check showed is under `Verified`.
Findings already rejected are not here.

{findings}

Rule on this set — severity relative to each other, duplicates, and what the
merge request amounts to — then call `submit_verdicts`.
