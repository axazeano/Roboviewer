# What a good review of nextcloud/ios#4091 finds — the reference the benchmark
# entry `ios-4091` is scored against.
#
# Format: one [[finding]] per claim that was checked against the code at the
# entry's head. `verdict = "expected"` is a defect the review has to find;
# `verdict = "false"` is a claim the review produced that was checked and
# proven wrong, with `why_false` saying why. Both are needed: without the false
# ones, high recall can be bought with noise — a match with one of them counts
# against precision, not towards recall.
#
# Every finding carries in `evidence` exactly what it was checked with. One
# without evidence is an opinion, not a reference — those do not go in here.
# `origin` says where the claim came from: "manual", collected by reading the
# code independently of any run, or "verified-from-run", taken from a run's
# output and then verified. The commits, size and language of the merge request
# are in items.toml, not repeated here.

[meta]
url = "https://github.com/nextcloud/ios/pull/4091"
established = "2026-08-04"    # findings 1-16, manual review
extended = "2026-08-20"       # findings 17-31, from muse-glimmer's output, verified
extended_2 = "2026-08-21"     # finding 32, from the muse-3-low-nobuild series
note = """
Careful when comparing models: findings 1-16 were collected independently of
any run, findings 17-32 come from muse-glimmer's output verified against the
code. The model that produced half of the reference has an advantage on it; an
honest comparison of models uses findings 1-16 only (`origin = "manual"`).
"""

# ---------------------------------------------------------------------------
# Part 1: collected by manual review on 2026-08-04, independently of any run of
# the tool. 13 of 16 are of the compiler's class: a build finds them, not a
# reviewer.
# ---------------------------------------------------------------------------

[[finding]]
id = "unresolved-searchMediaForAlbumUI"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Albums/Presentation/PhotoSelection/NCMediaViewRepresentable.swift"
line = 44
what = "media.searchMediaForAlbumUI() is called; the method is declared nowhere"
evidence = "grep -rn 'searchMediaForAlbumUI' *.swift yields a single occurrence — the call itself"

[[finding]]
id = "dismissSelect-lost-controller"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Media/NCMedia.swift"
line = 552
what = "dismissSelect lost its controller: parameter, so NCSelectDelegate is no longer satisfied"
evidence = "the signature in the protocol against the signature of the implementation"

[[finding]]
id = "getLayoutForView-wrong-label"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Media/NCMedia.swift"
line = 167
what = "getLayoutForView is called with the label layout:, and declared with layoutType:"
evidence = "the declaration is NCManageDatabase+LayoutForView.swift:92, label layoutType:"

[[finding]]
id = "unresolved-fileExistsQueue"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Media/NCMedia.swift"
what = "fileExistsQueue does not resolve"
evidence = "roboviewer pre-pass: no declaration in the repository"

[[finding]]
id = "unresolved-analytics"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Media/NCMedia.swift"
what = "AnalyticsHelper and MoEngageAnalytics are used and absent from the tree"
evidence = "roboviewer pre-pass; a grep over the repository is empty"

[[finding]]
id = "unresolved-updateRightBarButtonItems"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Main/NCMainTabBarController.swift"
line = 179
what = "NCMainNavigationController has no updateRightBarButtonItems"
evidence = "a grep for the declaration over the repository is empty"

[[finding]]
id = "unresolved-viewer-album-props"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Albums/Presentation/Details/PhotosGridView.swift"
line = 121
what = "NCViewerMediaPage is configured through the properties albumName / albumServerUrl / albumPhoto / metadatas, which it does not have"
evidence = "the declaration of NCViewerMediaPage carries none of these properties"

[[finding]]
id = "unresolved-popupFromNavigationStack"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 195
what = "popupFromNavigationStack is not declared, and is called on a freshly created UINavigationController()"
evidence = "the code: UINavigationController().popupFromNavigationStack(context:); no declaration"

[[finding]]
id = "mediahost-not-in-pbxproj"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "build"
file = "iOSClient/Media/NCMediaHost.swift"
line = 1
what = "The file is added, but no build manifest mentions it"
evidence = "grep -c NCMediaHost *.xcodeproj/project.pbxproj == 0"

[[finding]]
id = "more-scene-missing-storyboard"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "runtime-crash"
file = "iOSClient/Main/NCMainTabBarController.swift"
line = 94
what = "The More scene refers to NCMore.storyboard, which is not in the tree"
evidence = "roboviewer pre-pass: storyboard named here but no such file"

[[finding]]
id = "ncmedia-outlets-not-connected"
verdict = "expected"
origin = "manual"
severity = "blocker"
kind = "runtime-crash"
file = "iOSClient/Media/NCMedia.swift"
line = 209
what = "@IBOutlet titleConstraint and gradientViewHeightContsraint are added and absent from NCMedia.storyboard; viewDidLoad unconditionally writes titleConstraint.constant"
evidence = "NCMedia.storyboard is unchanged on the branch and carries neither name"

[[finding]]
id = "localization-keys-missing"
verdict = "expected"
origin = "manual"
severity = "major"
kind = "resources"
file = "iOSClient/Albums/Presentation"
what = "About 25 localization keys are used, none added to the strings files"
evidence = "roboviewer pre-pass: localization key used but defined in no strings file"

[[finding]]
id = "assets-missing"
verdict = "expected"
origin = "manual"
severity = "major"
kind = "resources"
file = "iOSClient/Main/NCMainTabBarController.swift"
line = 53
what = "The tab bar and the empty states refer to image names that are not in the asset catalogue"
evidence = "the names are found in no .xcassets"

[[finding]]
id = "copy-success-on-403-404"
verdict = "expected"
origin = "manual"
severity = "major"
kind = "error-handling"
file = "iOSClient/Albums/API/Albums+WebDAV.swift"
line = 453
what = "copyPhotoToAlbum returns success on statuses 403 and 404"
evidence = "the status-handling branch in the method body"

[[finding]]
id = "dead-deletePhotosFromAlbum-notification"
verdict = "expected"
origin = "manual"
severity = "minor"
kind = "dead-code"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 49
what = "An observer for the deletePhotosFromAlbum notification is registered, and nobody posts it"
evidence = "grep 'deletePhotosFromAlbum' — only addObserver, not a single post"

[[finding]]
id = "double-push-sheet-ondismiss"
verdict = "expected"
origin = "manual"
severity = "major"
kind = "correctness"
file = "iOSClient/Albums/Presentation/List/AlbumsListScreen.swift"
line = 53
what = "A double push: .sheet(onDismiss:) calls onPhotosSelected again, and navigation leaves before the copy completes"
evidence = "manual review 2026-08-04"

# ---------------------------------------------------------------------------
# Part 2: added 2026-08-20 from muse-glimmer's output, each verified against the
# code. Use for comparing models with care — see meta.note.
# ---------------------------------------------------------------------------

[[finding]]
id = "album-date-fallback-today"
verdict = "expected"
origin = "verified-from-run"
severity = "minor"
kind = "correctness"
file = "iOSClient/Albums/Domain/Models/Album.swift"
line = 55
what = "When decoding dateRange fails, startDate and endDate are set to Date() — broken data looks like \"today\""
evidence = "Album.swift:50-59, the else branch with let now = Date()"

[[finding]]
id = "environment-in-uikit-controller"
verdict = "expected"
origin = "verified-from-run"
severity = "minor"
kind = "correctness"
file = "iOSClient/Albums/AlbumsViewController.swift"
line = 14
what = "@Environment(\\.localAccount) is declared in a UIViewController — a SwiftUI wrapper outside SwiftUI is never populated"
evidence = "the class inherits UIViewController; viewDidLoad actually takes session.account"

[[finding]]
id = "deletePhotos-early-return"
verdict = "expected"
origin = "verified-from-run"
severity = "major"
kind = "correctness"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 175
what = "guard ... else { return } inside a for loop leaves the whole function: the remaining photos are not deleted"
evidence = "AlbumDetailsViewModel.swift:171-199, return instead of continue, and the same return on the first error"

[[finding]]
id = "deletePhotos-sequential-ui-churn"
verdict = "expected"
origin = "verified-from-run"
severity = "minor"
kind = "performance"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 171
what = "Deletion goes one photo at a time, toggling isLoadingPopupVisible on every iteration"
evidence = "a loop with await deletePhotoFromAlbum and MainActor.run per element"

[[finding]]
id = "deletePhotos-linear-search"
verdict = "expected"
origin = "verified-from-run"
severity = "minor"
kind = "performance"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 173
what = "photos.first(where:) inside the loop over metadatas — O(n*m)"
evidence = "AlbumDetailsViewModel.swift:173"

[[finding]]
id = "multiadd-spinner-hidden-early"
verdict = "expected"
origin = "verified-from-run"
severity = "major"
kind = "correctness"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 292
what = "isLoadingPopupVisible is set to true once, and every completed copy unconditionally sets it to false — the spinner goes away after the first of N"
evidence = "AlbumDetailsViewModel.swift:292 and :306"

[[finding]]
id = "notification-observer-leak"
verdict = "expected"
origin = "verified-from-run"
severity = "major"
kind = "lifecycle"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 49
what = "addObserver(forName:object:queue:using:) without keeping the token and without deinit — the block observer is never removed"
evidence = "the file has neither deinit nor removeObserver"

[[finding]]
id = "throwaway-navigation-controller"
verdict = "expected"
origin = "verified-from-run"
severity = "minor"
kind = "correctness"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 195
what = "UINavigationController() is created on the spot and a method is called on it — it has nothing to do with the screen's stack"
evidence = "AlbumDetailsViewModel.swift:195"

[[finding]]
id = "debug-print-file-name"
verdict = "expected"
origin = "verified-from-run"
severity = "nit"
kind = "logging"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 205
what = "print(\"DEBUG: Attempting to remove: ...\") with the name of a user's file, in production"
evidence = "AlbumDetailsViewModel.swift:205"

[[finding]]
id = "unused-setup-navigation"
verdict = "expected"
origin = "verified-from-run"
severity = "nit"
kind = "dead-code"
file = "iOSClient/Albums/Presentation/List/AlbumsListScreen.swift"
line = 108
what = "The view builder setupNavigation is declared and used nowhere"
evidence = "the only occurrence of the name in the file"

[[finding]]
id = "unused-showmedia-state"
verdict = "expected"
origin = "verified-from-run"
severity = "nit"
kind = "dead-code"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsScreen.swift"
line = 15
what = "@State showMedia is assigned and never read"
evidence = "two occurrences in the file: the declaration and the assignment"

[[finding]]
id = "window-polling-loop"
verdict = "expected"
origin = "verified-from-run"
severity = "major"
kind = "lifecycle"
file = "iOSClient/Albums/Presentation/PhotoSelection/NCMediaViewRepresentable.swift"
line = 39
what = "while media.view.window == nil polled every 50 ms; the Task is kept nowhere, so nobody can cancel it"
evidence = "NCMediaViewRepresentable.swift:32-45"

[[finding]]
id = "metadata-stub-db-write-from-view"
verdict = "expected"
origin = "verified-from-run"
severity = "major"
kind = "architecture"
file = "iOSClient/Albums/Presentation/Details/PhotosGridView.swift"
line = 64
what = "On every tap the full list is assembled and NCManageDatabase.shared.addMetadata writes stubs into the shared Realm straight from the view"
evidence = "PhotosGridView.swift:64-78"

[[finding]]
id = "repeated-sort-in-body"
verdict = "expected"
origin = "verified-from-run"
severity = "minor"
kind = "performance"
file = "iOSClient/Albums/Presentation/Details/PhotosGridView.swift"
line = 29
what = "photos.keys are sorted in body on every evaluation, and once more in openPhotoViewer"
evidence = "PhotosGridView.swift:29 and :56"

[[finding]]
id = "no-tests-for-albums"
verdict = "expected"
origin = "verified-from-run"
severity = "minor"
kind = "tests"
file = "iOSClient/Albums"
what = "Not one test covers the new feature"
evidence = "no file with test in its path mentions AlbumsListViewModel / AlbumsManager / AlbumMapper"

# Added 2026-08-21 from the muse-3-low-nobuild series (run 3), verified against the code.

[[finding]]
id = "copy-ocid-source-path"
verdict = "expected"
origin = "verified-from-run"
severity = "major"
kind = "correctness"
file = "iOSClient/Albums/Presentation/Details/AlbumDetailsViewModel.swift"
line = 299
what = """
sourcePath is built as metadata?.serverUrlFileName ?? photo, where photo is an
ocId. With no metadata, the COPY goes out with the ocId instead of the server
path, a request bound to fail. The same fallback in a second place:
AlbumsListViewModel.onPhotosSelected.
"""
evidence = "AlbumDetailsViewModel.swift:294-302 — a line above, photo is passed to getMetadataFromOcId(photo), so it is an ocId; AlbumsListViewModel.swift:212"

# ---------------------------------------------------------------------------
# False claims: the tool produced them, the judge confirmed them, verification
# refuted them. A match with one of these in a run counts against precision,
# not towards recall.
# ---------------------------------------------------------------------------

[[finding]]
id = "setuppasscodeview-requires-controller"
verdict = "false"
origin = "verified-from-run"
severity = "blocker"
file = "iOSClient/Main/NCMainTabBarController.swift"
line = 154
what = "SetupPasscodeView is created without its required controller argument — it does not compile"
why_false = """
SetupPasscodeView is a struct with no explicit init. `weak var controller: T?`
is an optional var property, and the synthesised memberwise initialiser gives
it a default of nil. The call compiles.
"""
evidence = "swiftc -typecheck on a minimal reproduction, exit 0"

[[finding]]
id = "mediahost-scene-identifier-wrong"
verdict = "false"
origin = "verified-from-run"
severity = "blocker"
file = "iOSClient/Media/NCMediaHost.swift"
line = 44
what = "sceneIdentifier defaults to the storyboard's name, and instantiateViewController will hit fatalError"
why_false = "iOSClient/Media/NCMedia.storyboard has storyboardIdentifier=\"NCMedia.storyboard\". The identifier is oddly named, but it exists."
evidence = "grep -o 'storyboardIdentifier=\"[^\"]*\"' iOSClient/Media/NCMedia.storyboard"

[[finding]]
id = "album-name-path-traversal"
verdict = "false"
origin = "verified-from-run"
severity = "major"
file = "iOSClient/Albums/API/Albums+WebDAV.swift"
line = 163
what = "The album name is substituted into a WebDAV path; traversal through .. or encoded separators is possible"
why_false = """
Validation strips / and \\, so .. cannot climb the tree; urlPath.encodedToUrl
percent-encodes the rest, so %2F becomes a literal.
"""
evidence = "validateAlbumName in AlbumsListViewModel.swift:77-88 and the path assembly in Albums+WebDAV.swift:163"
