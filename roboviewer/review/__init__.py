"""The review itself: what is asked of the agents and what is done with their answers.

Being assembled module by module — see the package docstring once `pipeline`,
`judge`, `merge`, `checklist` and `prompts` have moved in.
"""
